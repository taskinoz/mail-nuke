IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_USERNAME=you@example.com
IMAP_PASSWORD=your-app-password
IMAP_USE_SSL=true

IMAP_SOURCE_FOLDER=INBOX
IMAP_SPAM_FOLDER=Junk
IMAP_POLL_SECONDS=60

SPAM_THRESHOLD=0.90 # The threshold above which an email is considered spam.
MARK_SEEN_ON_SPAM=true # Whether to mark emails as seen when they are moved to the spam folder.
DRY_RUN=true # If true, actions will be logged but not executed. Set to false to actually move emails.
PROCESS_ONLY_UNSEEN=true # If true, only process emails that are unseen.

STATE_FILENAME=imap-worker-state.json
ACTION_LOG_FILENAME=imap-actions.jsonl

DEBUG=false # Set to true to enable debug logging, which includes listing IMAP folders on startup.

# Weekly feedback retraining. 604800 seconds is seven days.
RETRAIN_INTERVAL_SECONDS=604800
RETRAIN_RUN_ON_START=false
# Maximum absolute increase in ham false-positive rate allowed for promotion.
RETRAIN_FP_TOLERANCE=0.005
# Paths inside the container; override these for a host-run scheduler.
ACTION_LOG_PATH=logs/imap-actions.jsonl
# Messages restored here from Spam are learned as ham.
IMAP_FALSE_POSITIVE_FOLDER=INBOX
# Full mailbox bootstrap can use comma-separated folders for either label.
IMAP_HAM_FOLDERS=INBOX
IMAP_SPAM_FOLDERS=Junk
# Optional mail-stats integration. Leave blank when no dashboard is installed.
DASHBOARD_MARKS_FILE=
